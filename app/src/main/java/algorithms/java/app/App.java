/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package algorithms.java.app;

import algorithms.java.list.LinkedList;

import static algorithms.java.utilities.StringUtils.join;
import static algorithms.java.utilities.StringUtils.split;
import static algorithms.java.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
